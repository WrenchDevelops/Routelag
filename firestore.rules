rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Client SDKs are denied. PathGen server uses the Admin SDK, which bypasses rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
